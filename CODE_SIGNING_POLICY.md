# 코드 서명 정책

*한국어 · [English](CODE_SIGNING_POLICY.en.md)*

플랫폼마다 서명이 어떻게 되는지, 그 서명이 무엇을 보증하고 무엇은 보증하지 않는지 적어둔다.
서명된 프로그램을 실행하는 사람이라면 그 뒤에 뭐가 있는지 알아야 마땅하기 때문이다.

## 플랫폼별 현황

| 플랫폼 | 배포 경로 | 서명 |
| --- | --- | --- |
| Windows | Microsoft Store | **Microsoft가 서명한다** |
| macOS | GitHub 릴리즈 `.zip` | 없음; `SHA256SUMS.txt`만 제공 |
| 리눅스 네이티브 패키지 | GitHub Pages APT/DNF | **프로젝트 GPG 키로 서명** |
| 리눅스 휴대용 tar.gz | GitHub 릴리즈 | 없음; `SHA256SUMS.txt`만 제공 |

### Windows

Windows 서명 경로는 리눅스와 별개다. 개발자 계정은 승인되었지만 Store 인증과 공개 목록은 아직
대기 중이다. 제출한 MSIX가 인증을 통과하면 **Microsoft Store가 Microsoft 인증서로 다시 서명**하고
업데이트도 맡는다. 프로젝트 GPG 키는 Windows에 쓰지 않는다. `.exe`나 `.msi`를 직접 배포하는 경로도
없다.

### macOS

macOS `.zip` 아카이브는 Apple 개발자 서명이나 공증 없이 나간다. `SHA256SUMS.txt`는 내려받은 파일의
바이트가 릴리즈에 기록된 값과 같은지만 확인하며, 누가 만들었는지는 보증하지 않는다. Gatekeeper의
격리 경고를 처리하는 방법은 README와 릴리즈 노트에 둔다.

### 리눅스

APT와 DNF가 확인하는 대상은 다음과 같다.

| 서명 | 확인하는 것 | 체크섬의 역할 |
| --- | --- | --- |
| APT `InRelease` / `Release.gpg` | 저장소의 `Release` 메타데이터가 프로젝트 키로 서명되었고 바뀌지 않았는지 | 서명된 `Release`가 `Packages` 색인을, 그 색인이 `.deb` 파일을 해시로 묶는다 |
| RPM 패키지 서명 | 각 RPM 패키지의 서명자와 패키지 무결성 | 패키지 내부 파일 체크섬은 설치 뒤 파일 손상도 찾는다 |
| DNF `repomd.xml.asc` | `repomd.xml` 저장소 메타데이터가 프로젝트 키로 서명되었고 바뀌지 않았는지 | 서명된 repodata의 체크섬이 색인과 RPM 파일을 묶는다 |

그래서 DNF는 RPM 패키지 서명과 저장소 메타데이터 서명을 모두 확인한다. APT도 서명된 메타데이터에서
패키지까지 이어지는 체크섬 체인을 확인한다. 체크섬만 있는 macOS와 tar.gz 아카이브와 달리, 이
서명들은 어떤 프로젝트 키에서 왔는지도 확인한다. `SHA256SUMS.txt`는 아카이브 전송 오류 확인용이며
GPG 서명을 대신하지 않는다.

## 리눅스 공개 키

- URL: <https://devcode-kr.github.io/object-storage-client/repository-key.asc>
- 전체 지문: `843B0BB9F1A4488C8C7B60133F8AC712C8C56B90`
- 만료일: `2029-08-22` (생성일로부터 **3 years**)

설치 절차는 전체 지문과 키 구조를 확인하고 로컬 keyring에 넣는다. APT는 그 keyring을 해당 저장소의
`signed-by`에만 연결하고, DNF는 로컬 키 파일로 패키지와 메타데이터 확인을 모두 켠다.

개인키는 정확히 두 곳에만 둔다. 자동 배포용 복사본은 **GitHub Secrets**, 암호화된 복구본과 암호는
접근이 제한된 `/workspace/hermes_home/secure/object-storage-client-linux-repository/`에 둔다. 복구
저장소의 키 재료나 암호는 이 저장소, 빌드 산출물, 로그 또는 문서에 넣지 않는다.

## 키 교체

만료나 사고 대응으로 키를 바꿀 때는 **기존 키와 새 키**의 공개 키를 별도 URL로 먼저 게시해
**겹치는 기간**을 둔다. 사용자 keyring과 설치 문서가 두 키를 신뢰하도록 갱신하고 새 지문과 만료일을
문서에 반영한 뒤에만 APT 메타데이터, RPM 패키지, DNF 메타데이터 서명자를 새 키로 전환한다. 이전
패키지와 그 서명은 그대로 보존한다. 겹치는 기간이 끝나도 과거 패키지를 다시 서명하거나 삭제하지
않는다.

## 빌드와 배포

공식 Linux 저장소 서명은 이 저장소의 `v*` **태그**에서만 수행한다. PR은 실제 개인키에 접근하지
않고 **일회용 임시 키**로 동일한 패키지·저장소 검증 경로를 실행한다. 키 누락, 잘못된 지문, 만료,
서명 또는 검증 실패가 있으면 배포를 멈추며 **서명되지 않은 상태로 대체**하지 않는다. 검증을 통과한
저장소만 공식 GitHub Pages 배포 작업으로 게시한다. 개발자 PC에서 만든 저장소를 올리는 경로는 없다.

Windows MSIX는 같은 태그에서 빌드할 수 있지만 Partner Center와 Microsoft Store 인증·서명으로 가는
별도 경로다. Linux GPG 개인키로 MSIX에 서명하지 않는다.

## 역할

지금은 한 사람이 유지보수하고 있어서 모든 역할을 그 한 사람이 맡는다. 없는 직무 분리를 있는 것처럼
포장하지 않고 그대로 적는다.

| 역할 | 담당 | 하는 일 |
| --- | --- | --- |
| Author | Astral (메인테이너) | 소스 코드를 쓰고 커밋한다 |
| Reviewer | Astral (메인테이너) | `main`에 들어가기 전에 변경을 검토한다 |
| Approver | Astral (메인테이너) | 릴리즈와 Store 제출을 승인한다 |

메인테이너가 늘면 권한을 주는 그 커밋에서 이 표를 함께 고친다. 소스 저장소와 Partner Center에
접근하는 계정은 모두 다중 인증(MFA)을 켠다.

## 서명이 말해주는 것

서명은 파일이나 메타데이터가 해당 서명 주체를 거쳤고 그 뒤로 변조되지 않았다는 뜻이다. 잘
동작한다거나 안전하다는 보증은 아니다. 그 부분은 [LICENSE](LICENSE)의 면책 조항을 보면 된다.

## 개인정보와 문제 신고

[개인정보 처리방침](PRIVACY.md)에 정리해 두었다. 요약하면 아무것도 수집하지 않는다. 악성 동작이
의심되거나 이 정책과 맞지 않는 파일을 발견하면 저장소 이슈 트래커로 알려주기 바란다. 신고는
확인하고 원인은 공개한다. 설치와 제거 절차는 [README](README.md#설치)에 있다.
