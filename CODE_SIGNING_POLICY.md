# 코드 서명 정책

*한국어 · [English](CODE_SIGNING_POLICY.en.md)*

이 문서는 Object Storage Client 릴리즈에 누가 서명할 수 있고, 서명이 어떻게 이뤄지며, 그 서명이
무엇을 보증하는지 정리한 것이다. [SignPath Foundation](https://signpath.org)이 서명을 지원하는
프로젝트에 이런 문서를 요구하기도 하고, 서명된 프로그램을 실행하는 사람이라면 그 서명 뒤에 뭐가
있는지 알아야 마땅하기 때문이다.

## 서명 제공자

Windows 릴리즈는 [SignPath Foundation](https://signpath.org)이 발급하는 무료 인증서로
[SignPath.io](https://signpath.io)를 통해 서명할 예정이다.

**아직 서명된 릴리즈는 없다.** v0.0.1은 서명 없이 나갔고 Foundation 신청은 심사 중이다. 승인이
떨어진 다음 릴리즈부터 서명이 붙는다. 그전까지 이 문서는 이미 돌아가는 절차가 아니라 **앞으로 따를
절차**를 적어둔 것이다. 서명 없는 빌드를 처음 실행할 때 뜨는 경고는 릴리즈 노트에 설명해 두었다.

## 역할

지금은 한 사람이 유지보수하고 있어서 모든 역할을 그 한 사람이 맡는다. 없는 직무 분리를 있는 것처럼
포장하지 않고 그대로 적는다.

| 역할 | 담당 | 하는 일 |
| --- | --- | --- |
| Author | Astral (메인테이너) | 소스 코드를 쓰고 커밋한다 |
| Reviewer | Astral (메인테이너) | `main`에 들어가기 전에 변경을 검토한다 |
| Approver | Astral (메인테이너) | 태그된 릴리즈의 서명 요청을 승인한다 |

메인테이너가 늘면 권한을 주는 그 커밋에서 이 표를 함께 고친다. 그때 **Approver를 Author에서 먼저
떼어낸다.**

소스 저장소나 SignPath에 접근하는 계정은 모두 다중 인증(MFA)을 켜야 한다.

## 무엇에 서명하나

이 저장소의 `v*` 태그로 [`.github/workflows/release.yml`](.github/workflows/release.yml)이 빌드해
[GitHub 릴리즈](https://github.com/devcode-kr/object-storage-client/releases)에 올리는 **Windows
산출물만** 서명한다.

개발자 PC에서 서명하는 길은 없다. 이 저장소가 아닌 소스에서 나온 것도 서명하지 않는다. 서명된
바이너리에 이르는 길은 릴리즈 워크플로 하나뿐이고, 그 워크플로는 공개되어 있다. 어떤 파일이 어떤
빌드에서 나왔는지 Actions 로그에서 그대로 볼 수 있다.

서명하는 바이너리에는 제품명과 버전이 어셈블리 메타데이터로 박혀 있다. `Directory.Build.props`에서
설정하고, 태그와 그 버전이 어긋나면 릴리즈 워크플로가 먼저 실패한다. **버전을 잘못 달고 있는
빌드에는 서명이 붙을 수 없다는 뜻이다.**

## 서명이 말해주는 것

태그된 커밋의 소스로 자동 빌드된 바이너리이고, 그 뒤로 손대지 않았다는 것. 딱 여기까지다. 잘
동작한다거나 안전하다거나 정확하다는 보증은 아니다. 이 부분은 [LICENSE](LICENSE)의 면책 조항을
보면 된다.

릴리즈에는 `SHA256SUMS.txt`도 함께 올린다. 체크섬은 **파일이 온전히 도착했는지**를, 서명은 **어디서
왔는지**를 알려준다. 서로 다른 질문에 답하는 물건이다.

## 개인정보

텔레메트리도, 사용 통계도, 분석 데이터도 수집하지 않는다. 사용자가 직접 지정한 S3 호환 엔드포인트와,
설정했다면 HTTP 프록시. 그 밖으로는 아무것도 나가지 않는다.

자격증명은 마스터 비밀번호에서 뽑은 키로 암호화해서, 계정 주인만 읽을 수 있는 디렉터리에 둔다.
비밀번호는 디스크에 남기지 않는다. 이 프로젝트가 굴리는 계정도, 가입 절차도, 서버도 없다. 자세한
내용은 [데이터 저장 위치](README.md#데이터-저장-위치)에 있다.

## 설치와 제거

압축 파일로 배포하고 설치 관리자는 없다. 압축을 푼 폴더와 설정 디렉터리 하나 말고는 건드리지
않는다. 레지스트리에 쓰지 않고, 서비스를 깔지 않고, 시스템 설정을 바꾸지 않는다.

지우려면 압축을 푼 폴더를 지운다. 저장한 사이트와 설정까지 없애려면 Windows에서는
`%USERPROFILE%\.devcode\object-storage-client\`를, 다른 플랫폼에서는
`$HOME/.devcode/object-storage-client/`를 지우면 된다. [제거](README.md#제거)에 정리해 두었다.

## 문제 신고

악성 동작이 의심되거나 이 정책과 맞지 않는 서명이 붙은 파일을 발견하면 저장소 이슈 트래커로
알려주기 바란다. 신고는 확인하고, 원인은 공개한다.
