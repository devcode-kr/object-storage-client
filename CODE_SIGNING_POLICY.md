# 코드 서명 정책

*한국어 · [English](CODE_SIGNING_POLICY.en.md)*

이 문서는 Object Storage Client의 릴리즈에 **누가** 서명할 수 있고, 서명이 **어떻게** 이뤄지며,
그 서명이 무엇을 보증하고 무엇을 보증하지 않는지를 설명한다.
[SignPath Foundation](https://signpath.org)이 서명을 제공하는 프로젝트에 이 문서의 공개를
요구하기 때문이고, 서명된 바이너리를 실행하는 사람은 그 서명 뒤에 무엇이 있는지 알 자격이 있기
때문이다.

## 서명 제공자

Windows 릴리즈는 [SignPath Foundation](https://signpath.org)이 발급하는 무료 인증서로
[SignPath.io](https://signpath.io)를 통해 서명될 예정이다.

**아직 서명된 릴리즈는 없다.** v0.0.1은 미서명으로 배포되었고 Foundation 신청은 심사 중이다.
승인 이후의 첫 릴리즈부터 서명된다. 그때까지 이 문서는 이미 시행 중인 절차가 아니라 **앞으로
따를 절차**를 기술한 것이며, 미서명 빌드의 최초 실행 시 나타나는 경고는 릴리즈 노트에 설명되어
있다.

## 역할

현재 이 프로젝트는 한 사람이 유지보수하며, 따라서 모든 역할을 그 한 사람이 맡는다. 존재하지 않는
직무 분리를 있는 것처럼 포장하지 않고 그대로 밝힌다.

| 역할 | 담당 | 책임 |
| --- | --- | --- |
| Author | Astral (메인테이너) | 소스 코드 작성 및 커밋 |
| Reviewer | Astral (메인테이너) | `main`에 반영되기 전 변경 검토 |
| Approver | Astral (메인테이너) | 태그된 릴리즈의 서명 요청 승인 |

메인테이너가 늘어나면 접근 권한을 부여하는 바로 그 커밋에서 이 표를 갱신하며, **Approver를
Author에서 먼저 분리한다.**

소스 저장소나 SignPath에 접근하는 모든 계정은 다중 인증(MFA)을 사용해야 한다.

## 무엇이 서명되는가

이 저장소의 `v*` 태그로부터
[`.github/workflows/release.yml`](.github/workflows/release.yml)이 빌드하여
[GitHub 릴리즈](https://github.com/devcode-kr/object-storage-client/releases)의 자산으로
게시하는 **Windows 산출물만** 서명된다.

개발자 PC에서 서명하는 경로는 없으며, 이 저장소가 아닌 소스 트리에서 빌드된 것은 서명하지 않는다.
릴리즈 워크플로가 서명된 바이너리에 이르는 유일한 경로이고, 그 워크플로는 공개되어 있다 — 어떤
자산이 어떤 빌드에서 나왔는지 Actions 로그에서 확인할 수 있다.

서명되는 모든 바이너리는 `Directory.Build.props`에서 설정된 제품명과 버전을 어셈블리 메타데이터에
담는다. 태그와 그 버전이 어긋나면 릴리즈 워크플로가 실패하므로, **표기된 버전이 틀린 빌드에는
서명이 붙을 수 없다.**

## 서명이 의미하는 것

태그된 커밋 시점의 이 저장소 소스로부터 자동 빌드된 바이너리이며, 그 이후 변조되지 않았다는 것.
그것이 주장의 전부다. 적합성·보안성·정확성에 대한 보증이 아니다 — [LICENSE](LICENSE)의 면책
조항을 참고할 것.

릴리즈에는 `SHA256SUMS.txt`도 함께 제공된다. 체크섬 검증은 **파일이 온전히 도착했는지**를,
서명 검증은 **어디서 왔는지**를 알려준다. 서로 다른 질문에 대한 답이다.

## 개인정보

이 애플리케이션은 텔레메트리, 분석 데이터, 사용 기록을 일절 수집하지 않는다. 사용자가 설정한
S3 호환 스토리지 엔드포인트와, 설정한 경우의 HTTP 프록시 외에는 어디로도 아무것도 전송하지 않는다.

자격증명은 디스크에 결코 기록되지 않는 마스터 비밀번호로부터 파생된 키로 암호화되어, 계정 소유자만
읽을 수 있는 디렉터리에 저장된다. 이 프로젝트가 운영하는 계정도, 가입 절차도, 서버도 없다.
[데이터 저장 위치](README.md#데이터-저장-위치) 참고.

## 설치와 제거

압축 파일로 배포되며 설치 관리자가 없다. 압축을 푼 폴더와 설정 디렉터리 하나 외에는 아무것도
건드리지 않는다. 레지스트리에 쓰지 않고, 서비스를 설치하지 않으며, 시스템 설정을 변경하지 않는다.

제거하려면 압축을 푼 폴더를 지우면 된다. 저장된 사이트와 설정까지 지우려면 Windows에서는
`%USERPROFILE%\.devcode\object-storage-client\`, 그 외 플랫폼에서는
`$HOME/.devcode/object-storage-client/`를 삭제한다. [제거 방법](README.md#제거) 참고.

## 문제 신고

악성 동작이 의심되거나 이 정책과 맞지 않는 서명이 붙은 바이너리를 발견하면 저장소의 이슈 트래커로
신고할 것. 신고는 조사되며 원인은 공개된다.
