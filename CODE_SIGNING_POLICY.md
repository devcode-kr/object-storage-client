# 코드 서명 정책

*한국어 · [English](CODE_SIGNING_POLICY.en.md)*

플랫폼마다 서명이 어떻게 되는지, 그 서명이 무엇을 보증하고 무엇은 보증하지 않는지 적어둔다.
서명된 프로그램을 실행하는 사람이라면 그 뒤에 뭐가 있는지 알아야 마땅하기 때문이다.

## 플랫폼별 현황

| 플랫폼 | 배포 경로 | 서명 |
| --- | --- | --- |
| Windows | Microsoft Store | **Microsoft가 서명한다** |
| macOS | GitHub 릴리즈 | 없음 |
| 리눅스 | GitHub 릴리즈 | 없음 |

### Windows

Store로만 배포한다. 제출한 MSIX 패키지가 인증을 통과하면 **Microsoft가 자기 인증서로 다시
서명한다.** 그래서 이쪽에서 인증서를 살 일도, 패키지에 서명할 일도 없다. 사용자 입장에서는 경고를
넘기는 절차가 아예 없고 업데이트도 Store가 챙긴다.

`.exe`나 `.msi`를 직접 배포하지 않는다. 그건 Store가 다시 서명해주지 않아서 별도 인증서가 필요한데,
그 길을 택하지 않았다.

### macOS와 리눅스

서명하지 않는다. macOS에 서명하려면 Apple Developer Program(연 $99)이 필요한데 아직 들이지
않았다. 그래서 macOS는 처음 실행할 때 "손상되었기 때문에 열 수 없습니다"를 띄운다. 앱이 망가진 게
아니라 서명 없이 격리된 번들에 Gatekeeper가 붙이는 문구다. 넘어가는 방법은 릴리즈 노트에 있다.

리눅스는 막는 게 없다.

## 역할

지금은 한 사람이 유지보수하고 있어서 모든 역할을 그 한 사람이 맡는다. 없는 직무 분리를 있는 것처럼
포장하지 않고 그대로 적는다.

| 역할 | 담당 | 하는 일 |
| --- | --- | --- |
| Author | Astral (메인테이너) | 소스 코드를 쓰고 커밋한다 |
| Reviewer | Astral (메인테이너) | `main`에 들어가기 전에 변경을 검토한다 |
| Approver | Astral (메인테이너) | 릴리즈와 Store 제출을 승인한다 |

메인테이너가 늘면 권한을 주는 그 커밋에서 이 표를 함께 고친다. 소스 저장소와 Partner Center에
접근하는 계정은 모두 다중 인증(MFA)을 켠다.

## 무엇을 만들어 올리나

배포되는 것은 전부 이 저장소의 `v*` 태그로
[`.github/workflows/release.yml`](.github/workflows/release.yml)이 빌드한 결과다. 개발자 PC에서
만든 것을 올리는 길은 없다. 워크플로는 공개되어 있어서 어떤 파일이 어떤 빌드에서 나왔는지 Actions
로그로 확인할 수 있다.

Store에 올리는 MSIX도 같은 워크플로가 만든다. Partner Center에 올리는 일만 사람이 한다.

바이너리에는 제품명과 버전이 어셈블리 메타데이터로 박혀 있다. `Directory.Build.props`에서 설정하고,
태그와 그 버전이 어긋나면 워크플로가 먼저 실패한다.

## 서명이 말해주는 것

Windows 패키지에 붙은 Microsoft 서명은 **이 패키지가 Store를 거쳤고 그 뒤로 변조되지 않았다**는
뜻이다. 잘 동작한다거나 안전하다는 보증은 아니다. 그 부분은 [LICENSE](LICENSE)의 면책 조항을 보면
된다.

macOS와 리눅스 릴리즈에는 `SHA256SUMS.txt`를 함께 올린다. 체크섬은 파일이 온전히 도착했는지를
알려줄 뿐, 어디서 왔는지는 말해주지 않는다. 서로 다른 질문에 답하는 물건이다.

## 개인정보

[개인정보 처리방침](PRIVACY.md)에 정리해 두었다. 요약하면 아무것도 수집하지 않는다.

## 설치와 제거

Windows는 Store에서 설치하고 Store에서 제거한다. macOS와 리눅스는 압축 파일이라 푼 폴더를 지우면
끝이다. 어느 쪽이든 레지스트리에 쓰거나 서비스를 깔거나 시스템 설정을 바꾸지 않는다. 자세한 내용은
[제거](README.md#제거)에 있다.

## 문제 신고

악성 동작이 의심되거나 이 정책과 맞지 않는 파일을 발견하면 저장소 이슈 트래커로 알려주기 바란다.
신고는 확인하고, 원인은 공개한다.
