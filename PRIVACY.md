# 개인정보 처리방침

*한국어 · [English](PRIVACY.en.md)*

최종 수정: 2026-08-11

Object Storage Client(이하 "이 앱")는 **개인정보를 수집하지 않는다.** 아래는 그 말이 구체적으로
무엇을 뜻하는지 적은 것이다.

## 수집하지 않는 것

- 이름, 이메일, 계정 정보 등 신원을 알 수 있는 정보
- 사용 기록, 통계, 텔레메트리, 크래시 리포트
- 광고 식별자
- 파일 이름, 파일 내용, 접속한 스토리지의 목록

이 앱을 만든 쪽이 운영하는 서버는 없다. 가입 절차도 계정도 없다.

## 네트워크로 나가는 것

이 앱이 통신하는 상대는 **사용자가 직접 입력한 곳뿐이다.**

- 사용자가 설정한 S3 호환 스토리지 엔드포인트
- 사용자가 설정했다면, 그 HTTP 프록시

파일을 올리고 내리고 목록을 읽는 통신이 여기로 간다. 그 외 어디로도 아무것도 보내지 않는다.
업데이트 확인이나 원격 설정 같은 것도 하지 않는다.

Microsoft Store로 설치한 경우 Store 자체가 수집하는 정보는
[Microsoft 개인정보처리방침](https://privacy.microsoft.com/privacystatement)의 적용을 받는다. 이
앱이 관여하는 부분이 아니다.

## 컴퓨터에 저장되는 것

두 파일이 사용자 컴퓨터에만 저장된다. 어디로도 전송되지 않는다.

| 파일 | 내용 |
| --- | --- |
| `sites.json` | 저장한 접속 정보. 자격증명은 암호화 |
| `config.json` | 환경설정과 마스터 비밀번호 파라미터 |

위치는 `$HOME/.devcode/object-storage-client/`이고 Windows에서는 `%USERPROFILE%` 아래다. Store로
설치한 경우에도 같은 위치를 쓴다.

접속 정보는 엔드포인트와 리전, 액세스 키, 시크릿 키, 세션 토큰, 버킷, 프록시 설정까지 통째로 묶어
AES-256-GCM으로 암호화한다. 키는 마스터 비밀번호에서 PBKDF2-HMAC-SHA256을 600,000회 돌려 뽑고,
비밀번호 자체는 어디에도 적지 않는다. 운영체제가 지원하면 소유자만 읽을 수 있는 권한으로 파일을
만든다.

**마스터 비밀번호를 잊으면 되찾을 방법이 없다.** 복구용 사본을 어디에도 두지 않기 때문이다.

## 지우는 방법

앱을 지우면 프로그램 파일은 사라지지만 위 두 파일은 남는다. 다시 설치했을 때 설정을 잃지 않게
하려는 것이다. 완전히 없애려면 해당 디렉터리를 직접 지우면 된다.

```
Windows:  %USERPROFILE%\.devcode\object-storage-client\
macOS·리눅스:  ~/.devcode/object-storage-client/
```

## 어린이

이 앱은 어린이를 대상으로 하지 않으며, 어린이의 정보를 비롯해 누구의 정보도 수집하지 않는다.

## 방침이 바뀌면

이 문서를 고치고 최종 수정일을 갱신한다. 문서는 저장소에 있으므로 변경 내역이 커밋으로 전부
남는다.

## 문의

저장소 이슈 트래커로 남기면 된다.
<https://github.com/devcode-kr/object-storage-client/issues>
